# MongoDB
MONGO_URI=mongodb://localhost:27017/campuspay
# or MongoDB Atlas: mongodb+srv://<user>:<pass>@cluster.mongodb.net/campuspay

# Server
PORT=5000
CLIENT_URL=http://localhost:5173

# Firebase Admin SDK (download service-account JSON from Firebase Console
# -> Project Settings -> Service Accounts -> Generate new private key)
FIREBASE_PROJECT_ID=expense-manager-c7384
FIREBASE_CLIENT_EMAIL=your-service-account@expense-manager-c7384.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Google Cloud Vision (OCR) - needs a GCP project with Vision API enabled + billing
# Either point to a service account JSON file, or reuse the Firebase one if same GCP project
GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account.json

# UPI (for generating intent links, no real payment gateway needed for MVP)
DEFAULT_UPI_PAYEE_NAME=Tally+
